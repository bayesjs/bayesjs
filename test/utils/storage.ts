import * as expect from 'expect';
import {  } from '../../src/utils/index';


describe('utils', () => {
  describe('storage', () => {
    it('should store', () => {
      // const storage = createStorage(new WeakMap<object, any>());
      // const key = 'key';
      // const value = {
      //   name: 'Jonas',
      //   age: 22
      // };

      // storage.store(key, value);
      // expect(storage.isStored(key)).toBeTruthy();
      // expect(storage.getStored(key)).toEqual(value);
    });
  });
});